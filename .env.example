# Public client-side configuration only.
# Values prefixed with EXPO_PUBLIC_ are bundled into the app.

# EXPO_PUBLIC_API_URL=

# Server-side PostgreSQL connection used by Prisma.
# Never expose this value through an EXPO_PUBLIC_* variable.
# DATABASE_URL=
